{
  "name": "sales-playbook-backend",
  "version": "1.0.0",
  "description": "Minimal backend for the Sales Playbook Capture Studio: proxies adaptive question generation to GLM (Z.ai) and stores employee interview sessions so they're visible in one place.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
